<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <div class="nav-collapse">
        <ul class="nav pull-left">
          <li><%= link_to root_path do %><em class="fa fa-language fa-2x "></em><span>FPBS</span><% end %></li>
        </ul>
        <ul class="nav pull-right">
          <% if user_signed_in? %>
          <li><%= link_to root_path do %><em class="fa fa-connectdevelop fa-2x"></em><span>Home</span><% end %></li>
          <li ><%= link_to current_user do %><em class="fa fa-street-view fa-2x"></em>
          <span>Profile</span><% end %></li>
          <li><%= link_to mailbox_inbox_path do %>
          <%=unread_messages_count%>
          <em class="fa fa-envelope fa-2x"></em>
          <span>Inbox</span><% end %></li>
          <% end %>
          <% if user_signed_in? && lecturer? %>
          <li><%= link_to user_topics_path(current_user.id) do %><em class="fa fa-folder-open fa-2x"></em><span>Courses</span><% end %></li>
          <% end %>
          <% if user_signed_in? %>
          <li class="dropdown" >
                  <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                      <em class="fa fa-cog fa-2x"></em></strong>
                      <b class="caret"></b>
                  </a>

                  <ul class="dropdown-menu">
                    <li><%= current_user.name %></li>
                    <% if user_signed_in? and current_user.role.name=="Admin" %>
		    <li><%= link_to "Manage User", users_path, :class => "btn btn-default" %></li>
                    <li><%= link_to "Manage Role", roles_path, :class => "btn btn-default" %></li>
		    <li><%= link_to "Manage Classroom", classrooms_path, :class => "btn btn-default" %></li>
                    <% end %>
                    </li>
                    <li><%= link_to destroy_user_session_path, :method => :delete do %>
                    <em class="fa fa-power-off fa-1x"></em><span>Logout</span>
                    <% end %>
                    </li>
                  </ul>
                <% else %>
         <li class="dropdown">
                  <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                      <strong><em class="fa fa-power-off fa-2x"></em></strong>
                      <b class="caret"></b>
                  </a>
         <ul class="dropdown-menu">
         <li><%= link_to "Login", new_user_session_path %></li>
       <% end %>
          </ul>
        </ul>
      </div>
    </div>
  </div>
</div>
